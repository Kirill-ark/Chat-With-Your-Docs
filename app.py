"""Phase 5 — Streamlit chat UI.

Upload PDFs -> ingest; type a question -> retrieve -> generate. Every answer
shows its sources (filename, page) and similarity scores underneath, because
transparency about *where* the answer came from is the whole point of RAG.

API keys come from env vars / hosting secrets only.

TODO (Phase 5): build the upload + chat interface.
"""

# TODO: implement in Phase 5
